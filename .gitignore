# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

deployment/services/api/config.yml
deployment/services/dataharvester/config.yml
deployment/services/dirserver/config.yml